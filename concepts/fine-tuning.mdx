---
title: Fine-tuning
description: Guide to fine-tuning models with Magemaker
---

## Fine-tuning Overview

Fine-tuning allows you to adapt pre-trained models to your specific use case.  
**Today, fine-tuning is only supported on AWS SageMaker** (Vertex AI & Azure ML support are on the roadmap).

<Warning>
  You’ll need sufficient **SageMaker training quota** in the region where you plan to train.  GPU instances (e.g. `ml.p3.*`) are not enabled by default on new AWS accounts.
</Warning>

---

## Basic Workflow

1. Prepare & upload your dataset to **S3** (CSV or JSONL – see below).  
2. Create a YAML config that contains a `!Training` block and at least one `!Model` block.  
3. Run:

```bash
magemaker --train .magemaker_config/train-config.yaml
```

Magemaker will validate the YAML, start an **SageMaker Training Job** and stream logs to your terminal.

---

## Configuration Reference

### Minimal Training Configuration

```yaml
training: !Training
  destination: aws               # only "aws" is supported for now
  instance_type: ml.p3.2xlarge    # 1× V100 GPU
  instance_count: 1               # distributed training coming soon
  training_input_path: s3://my-bucket/my-data.csv  # dataset location

models:
- !Model
  id: google-bert/bert-base-uncased   # Hugging Face model ID
  source: huggingface
```

### Advanced Example

```yaml
training: !Training
  destination: aws
  instance_type: ml.p3.8xlarge        # 4× V100 GPUs
  instance_count: 1
  training_input_path: s3://my-bucket/data.jsonl
  hyperparameters: !Hyperparameters
    epochs: 3
    per_device_train_batch_size: 32
    learning_rate: 2e-5
    weight_decay: 0.01
    warmup_steps: 500
    evaluation_strategy: "steps"
    eval_steps: 500
    save_steps: 1000

# You can fine-tune **any** model that supports text training on HF
models:
- !Model
  id: meta-llama/Meta-Llama-3-8B-Instruct
  source: huggingface
  task: text-generation
```

<Note>
  `training_input_path` **must** start with `s3://` – local paths are not yet supported.  A helper to automatically upload local files is planned.
</Note>

---

## Data Preparation

### Supported Formats

<CardGroup>
  <Card title="CSV" icon="file-csv">
    - Simple tabular format – one row per training example  
    - Ideal for classification & regression
  </Card>
  <Card title="JSON Lines (JSONL)" icon="file-code">
    - One JSON object **per line**  
    - Flexible structure – recommended for generative tasks
  </Card>
</CardGroup>

### Uploading to S3

<Steps>
  <Step title="Prepare Data">
    Ensure the dataset matches the expected input format for your model / task.
  </Step>
  <Step title="Upload to S3">
    ```bash
    aws s3 cp my-data.csv s3://my-bucket/my-data.csv
    ```
  </Step>
  <Step title="Reference in YAML">
    Use the full `s3://…` URI in `training_input_path`.
  </Step>
</Steps>

---

## Choosing Instance Types

| Instance | GPUs | Typical Use-case |
|----------|------|------------------|
| `ml.p3.2xlarge`  | 1× V100 | small-to-medium datasets |
| `ml.p3.8xlarge`  | 4× V100 | larger datasets / faster training |
| `ml.p3.16xlarge` | 8× V100 | distributed training |

<Warning>
  GPU capacity is region-specific.  If you hit *CapacityError* try another region or request a quota increase.
</Warning>

---

## Hyperparameter Shortcuts

If you omit `hyperparameters`, Magemaker will call `get_hyperparameters_for_model()` to generate sensible defaults for common text tasks (batch size, learning-rate schedule, etc.).  You can override **any** of them in the YAML file.

---

## Monitoring Your Training Job

1. Real-time logs stream to your terminal.  
2. Detailed metrics are pushed to **CloudWatch** → *SageMaker / TrainingJobs* namespace:
   - `TrainingLoss`
   - `ValidationLoss`
   - `GPUUtilization`

Set up CloudWatch alarms to catch OOM or stalled jobs.

---

## Next Steps

- Use the trained model artefact to **deploy a new endpoint** with another YAML file (replace `training:` with `deployment:`).  
- Automate training & deployment in CI/CD by chaining `--train` and `--deploy` commands.

<Note>
  Vertex AI & Azure ML fine-tuning support is under active development.  Follow the progress on our [GitHub Issues](https://github.com/slashml/magemaker/issues).
</Note>
